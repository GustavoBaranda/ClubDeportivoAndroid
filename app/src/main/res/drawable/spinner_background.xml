<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo cuando el Spinner está deshabilitado -->
    <item android:drawable="@drawable/spinner_shape_disabled" android:state_enabled="false" />
    <!-- Fondo cuando el Spinner está habilitado -->
    <item android:drawable="@drawable/spinner_shape" />
</selector>

